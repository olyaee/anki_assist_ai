[tool.poetry]
name = "anki-word-processor"
version = "1.0.0"
description = "Clean implementation for processing German vocabulary words and syncing to Anki"
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
google-genai = "^1.0.0"
pandas = "^2.0.0"
pillow = "^10.0.0"
requests = "^2.31.0"
pyyaml = "^6.0"
python-dotenv = "^1.0.0"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
